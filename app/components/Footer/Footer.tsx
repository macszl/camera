import {View} from 'react-native';

export function Footer() {
  return (
    <View
      style={{
        width: '100%',
        alignItems: 'flex-end',
        marginBottom: 40,
        marginRight: 40,
      }}
    />
  );
}
